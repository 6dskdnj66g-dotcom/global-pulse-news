# Global Pulse | النبض العالمي

A premium, responsive news website built with React, Vite, and TypeScript.

## 🚀 Getting Started

Since Node.js was not detected on your system, please follow these steps to run the project:

### 1. Install Node.js
Download and install the **LTS version** of Node.js from [nodejs.org](https://nodejs.org/).

### 2. Install Dependencies
Open your terminal (Command Prompt or PowerShell), navigate to this folder, and run:
```bash
cd desktop/global-pulse-news
npm install
```

### 3. Run Development Server
To see the site locally:
```bash
npm run dev
```
Open the URL shown in the terminal (usually `http://localhost:5173`) in your browser.

## 🌐 Deployment

You can deploy this site for free using Vercel or Netlify.

### Option A: Deploy to Vercel (Recommended)
1. Install the Vercel CLI: `npm i -g vercel`
2. Run `vercel` in this directory.
3. Follow the prompts (hit Enter for defaults).
4. Your site will be live in minutes!

### Option B: Drag and Drop (Netlify)
1. Run `npm run build` to create a `dist` folder.
2. Go to [Netlify Drop](https://app.netlify.com/drop).
3. Drag the `dist` folder onto the page.

## 🛠 Features
- **Responsive Design**: Works on Mobile, Tablet, and Desktop.
- **Premium UI**: Smooth animations, glassmorphism effects, and professional typography.
- **Dynamic Content**: Category filtering, detailed article views, and breaking news ticker.
- **Bilingual Ready**: Structure supports RTL (Arabic) and LTR (English).
